
#type Query
#}

type Mutation {
    createUser(userInput: UserInput): User
}

type User {
    id: String!
    email: String!
    name: String
}

input UserInput {
    id: String!
    email: String!
    name: String
}