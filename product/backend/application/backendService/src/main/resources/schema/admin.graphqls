directive @admin on FIELD_DEFINITION

extend type Query {
    getUserStats: UserStats @admin
    getAllUsers: [User]
}


type UserStats {
    total: Int
    newUserTotal: Int
    deletedUserTotal: Int
}