extend type Query {
    getCurrentConversation(conversationId: String!): [Chat]
}

extend type Mutation {
    createChat(conversationId: String!, message: String!): MutationResponse
    clearCurrentConversation(conversationId: String!): MutationResponse
}

extend type Subscription {
    chatSubscription(conversationId: String!): Chat
}

type Chat {
    conversationId: String!
    chatId: String!
    userId: String
    sender: String!
    message: String!
    createdAt: String
}
