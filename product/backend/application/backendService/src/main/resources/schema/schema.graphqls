type Mutation {
    
}

type Query {

}

input PaginationInput {
    page: Int
    size: Int
}

type Pagination {
    page: Int
    size: Int
    total: Int
}

type MutationResponse {
    success: Boolean
    message: String
}

enum SortOrder {
    ASC
    DESC
}