type Mutation {
    createUser(userInput: UserInput): User
}

input UserInput {
    id: String!
    email: String!
    name: String
}

type User {
    id: String!
    email: String!
    name: String
}