type Mutation {
    
}

type Query {

}

type Subscription {

}

input PaginationInput {
    page: Int
    size: Int
}

input SortInput {
    sortBy: String
    sortDirection: SortDirection
}

type Pagination {
    page: Int
    size: Int
    total: Int
}

type MutationResponse {
    success: Boolean
    message: String
}

type Sorting {
    sortBy: String
    sortDirection: SortDirection
}

enum SortDirection {
    ASC
    DESC
}