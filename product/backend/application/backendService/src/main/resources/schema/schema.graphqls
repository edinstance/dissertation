type Mutation {
    
}

type Query {

}

input PaginationInput {
    page: Int
    size: Int
}

input SortInput {
    sortBy: String
    direction: SortDirection
}

type Pagination {
    page: Int
    size: Int
    total: Int
}

type MutationResponse {
    success: Boolean
    message: String
}

type Sorting {
    sortBy: String
    direction: SortDirection
}

enum SortDirection {
    ASC
    DESC
}